import { BooleanToSimNaoPipe } from './boolean-to-sim-nao.pipe';

describe('BooleanToSimNaoPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanToSimNaoPipe();
    expect(pipe).toBeTruthy();
  });
});
